# 当前下一步建议

- P2：Mermaid 代码块渲染（优先实现，支持 ```mermaid``` 代码块在预览区转为流程图 / 图表）
- P2：ECharts 代码块渲染（支持 ```echarts``` 代码块，解析 option 并渲染图表）
- P2：特殊代码块渲染分发器（统一识别代码块类型，方便后续继续接入更多渲染器）
- P3：图片上传体验补强（拖拽上传、点击上传、上传失败重试、上传中占位优化）

# 新会话交接提示

- 本会话已完成：登录 / 注册、双 Token 鉴权、Dashboard 文件夹与文档 CRUD、编辑页骨架、自动保存 / 手动保存、TOC 自动生成与导航、Markdown 列表 / 高亮 / 公式修复、图片粘贴上传。
- 当前图片上传链路已打通：前端粘贴图片后先压缩，再调用 `POST /api/v1/uploads/images`，服务端将文件保存到 Docker 容器内 `/app/data/uploads`，并通过 `/uploads/...` 相对路径公开访问。
- Docker 已改为使用根目录 `data/uploads` 作为宿主机挂载目录，`data/` 已加入忽略规则；端口区间统一为 58000-58099。
- 当前前端风格已统一为浅暖色主题：页面背景 `#FAF9F5`，主文字 `#3D3D3A`，hover `#E8E6DC`，选中色 `#F3EADF`，交互组件尽量收口到 shadcn/ui。
- 当前重要约束：
  - 数据库表绝对不要使用物理外键，只能使用逻辑外键。
  - 所有中文文档、中文注释、中文文案文件统一使用 UTF-8 with BOM。
  - 完成任务后要主动给出下一步建议，并在用户同意后覆盖更新本文件。
- 给下一个会话的建议开场指令：
  - 请先阅读 `AGENTS.md` 与 `docs/plan.md`。
  - 然后只实现第一个 P2 小任务：Mermaid 代码块渲染，不要一次扩太大。
