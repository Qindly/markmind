# 当前下一步建议

- P1：实现编辑器选区 AI 助手首版闭环
- 范围：新增用户级 AI 设置页，支持 OpenAI Compatible Provider 的 `baseURL`、`apiKey`、`model` 配置；编辑器选中文段后出现“魔法笔 / 中英翻译”两个悬浮快捷入口，并通过后端代理完成局部润色与双语翻译
- 目标效果：用户可以在不离开编辑器的前提下，对任意 Markdown 选区做局部 AI 改写或翻译，并对结果执行替换、插入、复制、取消，同时保证 Provider 密钥不暴露在浏览器端
